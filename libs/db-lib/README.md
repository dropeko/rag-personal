# db-lib

This library was generated with [Nx](https://nx.dev).

## Building

Run `npx nx build db-lib` to build the library.

## Running unit tests

Run `npx nx test db-lib` to execute the unit tests via [Jest](https://jestjs.io).
